import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
	return (
		<div>
			<Head>
				<title>devCecy log</title>
				<meta name="description" content="devCecy의 블로그입니다." />
				<link rel="icon" href="/favicon.ico" />
			</Head>

			<div>cecy log start!</div>
		</div>
	);
};

export default Home;
